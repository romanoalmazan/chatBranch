# Google Cloud / Vertex AI Configuration
GCP_PROJECT_ID=branchchat-481006
GCP_LOCATION=us-central1
GEMINI_MODEL_NAME=gemini-2.5-flash

# Server Configuration
PORT=3000

# Authentication
# Option 1: Service Account Key File (used for both Firestore and Firebase Admin)
GOOGLE_APPLICATION_CREDENTIALS=./chatbranch-service-account.json

# Option 2: API Key (alternative to service account)
# GEMINI_API_KEY=your-api-key-here

# Firebase Configuration
# Note: Firebase Admin uses the same service account as Firestore (GOOGLE_APPLICATION_CREDENTIALS)
# The project ID should match your GCP_PROJECT_ID
FIREBASE_PROJECT_ID=your-firebase-project-id
